import "../styles/index.css";

export default function SettingsPanel() {
	return (
		<section id="settings" className="settings-panel">
			<h2>Settings</h2>
			<p>Configure your emulator path and preferences here.</p>
		</section>
	);
}
